# locale.conf
---
region: "Europe"        # default timezone region (starting point on the map)
zone: "London"          # default timezone zone
localeGenPath: "/etc/locale.gen"
# GeoIP timezone lookup is DISABLED (no geoip: block below).
# NOTE: the old "geoipUrl"/"geoipStyle" keys were IGNORED by the 3.4.x locale
# module - it only reads a "geoip:" submap. To enable the lookup instead:
# geoip:
#   style: "json"
#   url: "https://geoip.kde.org/v1/calamares"
#   selector: ""
