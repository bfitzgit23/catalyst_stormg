# /etc/calamares/modules/hwclock.conf
#
# The hwclock module (3.3.x and 3.4.x) is a Python module that sets the
# hardware clock (hwclock --systohc --utc) and takes no configuration keys.
# This file exists only so calamares does not print a "No config file for
# hwclock" warning in the session log; keep it even though it is empty.
---
