# /etc/calamares/modules/removeuser.conf
#
# Removes the "gentoo" live user from the installed system (userdel -r).
# NOTE: this file used to start with a leading space (" username: gentoo"),
# which is invalid YAML for most parsers and could make the module load with
# an empty config - the module then cannot find a username to remove.
username: gentoo
